# Public pull-request proof

This folder compares two real branches from the public Tripwire repository. Both
Passports were generated by engine commit `1bb623ae5bc1df16f60b566b15d4eb1f02e1ac78`
through the same `assess-git` path used by pull-request automation.

| Public change | Executed result | Evidence |
|---|---|---|
| [PR #1: unknown devices become trusted](https://github.com/chinnuteja/datahub-tripwire/pull/1) | `UNSAFE`; model and agent behavior change for `TX-009`; GitHub Check fails | [`unsafe-pr-passport.json`](unsafe-pr-passport.json), [`unsafe-pr-check.md`](unsafe-pr-check.md) |
| [PR #2: nullable reviewer note](https://github.com/chinnuteja/datahub-tripwire/pull/2) | `SAFE_WITHIN_SCOPE`; 2/2 critical consumers pass; no witness | [`safe-pr-passport.json`](safe-pr-passport.json), [`safe-pr-check.md`](safe-pr-check.md) |

The unsafe Passport goes beyond diagnosis. Tripwire derived the changed null-handling
expression, proposed the smallest repair, executed the repaired SQL, and verified that
both the fraud-model predictions and Fraud Review Agent decisions returned to their
baseline outputs. The report includes the exact patch and fixed-output hash.

Both Passports also expose auditable scope accounting and DataHub-derived review routing:

- metadata operations completed versus required;
- critical consumers evaluated versus discovered;
- unresolved context gaps and lineage-frontier status;
- the DataHub owner responsible for review.

They execute `fraud-risk-calibrator/2.0.0` from a typed logistic-regression artifact and
record its SHA-256 digest, the exact model version, and all 12 replayed rows in each
critical-consumer evaluation.

The context file used for this repository-safe proof is explicitly recorded and contains
a sanitized, source-hashed ownership fact from the seeded DataHub graph. The separate
[`learned-loop`](../learned-loop/README.md) artifacts preserve the live MCP read and
DataHub write-back evidence.
